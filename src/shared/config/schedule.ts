export const enum Sizes {
    MAX_PERCENT_WIDTH = 100,
    MIN_HEIGHT_SCHEDULE_BLOCK = 32,
    MIN_WIDTH_SCHEDULE_BLOCK = 12,
    HIDE_WIDTH_CONTENT_SCHEDULE_BLOCK = 30,
    WIDTH_SCHEDULE_BLOCK_EXTENSION = 4,
    /** The indentation between the blocks, when changing the value, requires changes in styles. */
    INDENT_SCHEDULE_BLOCK = 2,
    /** The height of one row (60 minutes). */
    ROW_HEIGHT = 68,
    HEIGHT_OF_TEMPLATE = 68 * 24
}
